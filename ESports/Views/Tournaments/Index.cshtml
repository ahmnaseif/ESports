@model IEnumerable<ESports.Models.Tournament>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TrId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TrId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TrId })
        </td>
    </tr>
}

</table>
